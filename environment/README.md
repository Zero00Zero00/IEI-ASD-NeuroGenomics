# Environment

`../environment.yml` provides a portable baseline environment for the public analysis code.

Exact software versions used during the executed project stages are preserved under `stage_versions/` and should be treated as the historical execution record.

MAGMA is an external executable and is not bundled in the Conda environment. The version and invocation used by the project are documented in the archived stage records and analysis scripts.
