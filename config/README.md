# Configuration

The executable configuration files used by each analysis stage are archived together with their corresponding stage code under:

`scripts/stages/<stage>/config/`

This top-level directory intentionally does not duplicate those files.

Analysis-lock JSON files are stored under `protocol/locks/`.
