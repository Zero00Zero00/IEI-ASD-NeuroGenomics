# Figures

This directory contains the final vector manuscript figures.

`main/` contains Figures 1-5.

`extended/` contains Extended Data Figures 1-9.

Numerical figure source tables are stored under `source_data/`.

PDF and SVG are retained as the publication vector formats. High-resolution raster exports and internal rendering-QC contact sheets are intentionally excluded from the Git repository.
